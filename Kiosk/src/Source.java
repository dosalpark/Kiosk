public class Source extends Product {
    private int idx;
    private String name;
    private String description;
    private Double price;

    public Source(int idx, String name, String description, Double price) {
        super(idx, name, description, price);
    }
}
